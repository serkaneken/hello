# Hello, GitHub


## Setup

```
brew install ruby (if doesn't exist)
gem install bundler
bundle install
```

## Local dev

```
bundle exec jekyll serve
```

Then, open browser to <http://localhost:4000>.
